# 点击系统菜单确认按钮时打开页面、确认重新开始或结束游戏、保存按键 `0x0044BDA0`

状态：`platform_adapted`

## 1. LST锁

权威范围为`swd3.exe.lst`的`0x0044BDA0..0x0044C0AA`，无外部FUNCTION CHUNK。七个直接caller均位于`0x0044B070`，入口另由游戏内菜单回调表保存。

函数首先读取完整输入锁；非零时原值返回，不修改菜单。其余行为按当前页面执行：

- 顶层页面0：先查询物品18，再查询当前MAPS值分组；任一不可用或查询后目标变为22时切到不可用提示并播放提示音，否则提交第一种页面请求并重置菜单工作区。
- 顶层页面1：提交第二种页面请求并重置菜单工作区。
- 顶层“记载”页：清空三项页面状态后打开记载清单。
- “离开”页：第0/1行分别选择“结束游戏”或“重新开始”；菜单flag bit1存在时锁定输入并把确认值写30，否则写0。
- 设置页面第6行：复制完整32 dword按键块到编辑区，打开保存/取消/恢复默认操作列表。
- 重新开始/结束游戏确认：光标值非零时返回“离开”页；为零时锁定输入、写倒计时30、顺序执行开始与完成回调，再清空六项转场值。
- 按键操作16：先把编辑区完整复制回运行时块，再保存并返回设置页面。
- 按键操作17：取消编辑并返回设置页面。
- 按键操作18：先清空运行时块，恢复默认按键，再把恢复后的完整块复制到编辑区。
- 其他固定操作：播放样本107并进入页面6；页面6确认后进入页面7；页面10确认后返回顶层页面0。

所有页面保留原程序写状态、查询、回调、复制和提示音的顺序。未关闭的菜单工作区、详情动画及按键持久化callee以最窄typed命令隔离，不伪造其内部结果。

## 2. caller回收

`0x0044B070`的七个确认位置已删除旧`commit`命令，直接调用`confirm_legacy_system_menu_selection`并合并helper计数、返回值和最后一条命令。顶层“记载”页确认返回后仍重读音效索引，再播放原提示音。

## 3. 验证

UT覆盖输入锁、三个顶层页面、物品18短路、值分组查询后目标重读、重新开始/结束游戏入口及确认转场顺序、完整按键块编辑/保存/取消/默认恢复、固定操作、页面6/7/10以及七个B070 caller。

workpack双生成稳定为`176/227`，SHA256为`97aae605dc499e89da29e74a5eead9dca4e1178c01f7769963a5a0bea10c21d0`；下一项为`0x0044C0E0`。
