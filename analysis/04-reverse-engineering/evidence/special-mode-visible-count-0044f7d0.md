# 统计特殊模式工作区最多13条可见记录 `0x0044F7D0`

状态：`assembly_exact`

## 1. LST锁

权威范围为`swd3.exe.lst`的`0x0044F7D0..0x0044F7F3`，24行，无callee、无外部FUNCTION CHUNK。caller为四个待回收的选择/翻页函数。

入口无条件把共享可见数清零。head为null时立即返回null。非null时循环：

1. 在前进前检查当前计数是否已达到13；达到则返回当前指针。
2. 计数加一并立即发布。
3. 读取当前记录next；非null继续。

因此14条链返回计数13和已读取到的第14条指针。恰好13条时，第13次仍读取next=null后返回。原函数有固定上限，自环会把同一记录重复计数13次并返回同一指针；不能添加环停止或去重。

## 2. 验证

UT覆盖null head、14条链的13上限和第14条返回指针，以及自环重复计满13。

workpack双生成稳定为`198/227`，SHA256为`91b8269aa1aaa33e09421c0674ca10a7ff6b306fa1e75f3a7f4417d58c22e628`。`0x0044DBC0`继续等待callee闭环；下一依赖叶子为`0x0044F8E0`。
