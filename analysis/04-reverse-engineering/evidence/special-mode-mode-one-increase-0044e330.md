# 模式1按右向操作增加当前值 `0x0044E330`

状态：`platform_adapted`

## 1. LST锁

权威范围为`swd3.exe.lst`的`0x0044E330..0x0044E48A`，186行，无外部FUNCTION CHUNK。caller为`0x0044DBC0`和`0x0044F920`。函数按level 1..4分派；其他level无副作用并保留`level-1`返回域。

## 2. 四层行为

- level1：打包模式低两位加1并夹到2，只清写低两位。
- level2：按`window+cursor`的32位结果执行B9A0 signed选择，先把当前记录`combined_value(+6)`按u16加1。
- level3：设置打包低字节bit2。
- level4：设置打包低字节bit3。

## 3. level2上限顺序

packed bit0为0：

1. 调用已纠正的F770，以工作区每条记录`+0x52`权重乘`combined_value(+6)`及100倍率计算总权重。
2. 将F770的32位结果按u32与容量比较。超限时先撤销本次加1，再把level写10，播放样本`0x008C`；不发布右向动作状态。
3. 未超限则调用FA40，按当前记录ID汇总玩家链和固定装备贡献。
4. 计算`99-contribution`的32位结果；当前u16数量零扩展后按i32与其比较。当前数量不小时，把数量写为该差值低16并发布右向动作状态2，不播放B9；否则保留加值、播放样本`0x00B9`并发布状态2。

packed bit0为1：

- 把记录`first_value(+8)`与`second_value(+0x0A)`分别按i16扩展后做32位加法，作为比较上限。
- 加值后的u16数量零扩展后若按i32大于该上限，数量改写为两字段word加法低16，发布状态2且不播放声音；否则保留加值、播放B9并发布状态2。

## 4. typed-stop与验证

B9A0选择、F770链环和FA40表/链边界只在原读取/调用点停止。F770或FA40停止时保留已经增加的数量，不伪造后续动作状态。记录上限夹取的原EAX记录指针通过typed标记表达。

UT覆盖低两位夹2；普通增加后总权重与装备贡献检查、B9和状态2；重量超限撤销数量、level10及样本8C；装备占用把99夹到98；bit0模式把6夹回自身上限5及未超限B9；level3/4置位；自环在F770停止并保留加值2。

workpack双生成稳定为`210/227`，SHA256为`3de68a8186ee51563818512854d394b45ba507ffe383bb8d1fbaef25a7ef0e42`。`0x0044DBC0`继续等待callee闭环；下一单元为`0x0044E4A0`。
