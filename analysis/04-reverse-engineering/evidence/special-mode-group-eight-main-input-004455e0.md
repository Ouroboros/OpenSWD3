# G08主输入编排 `0x004455E0`

状态：`platform_adapted`、`unit_tested`

唯一行为真值为`swd3.exe.lst`。物理范围`0x004455E0..0x00445C8C`，829行，无FUNCTION CHUNK。无code caller；3B480把它安装为G08主输入callback。

函数先snapshot交互模式。模式大于等于500时直接复用已关闭C3C0运行时输入分派并返回。其余路径先清input-consumed；模式17/18任一低四输入位会直接归一到模式2。

模式10按严格矩形选择25像素外层行，模式11按当前外层行选择66像素列，模式5按当前记录行选择44像素动作；确认均直接进入446700端口，退出位进入446FE0端口。模式10/11退出callback后严格重读mode与input flags；模式2 overlay callback后同时重读mode、flags和坐标。

模式2顶部选择保留两个不同公式：60像素列判定31特例；普通项先按6像素缩放、再限定十进制余数0..7并发布选择。31特例按LST临时写31、提交、恢复30、播放45并发布31；普通变化播放46。hover比较仍使用`offset>>5`，写入仍使用`offset/33+1`，不修正原始不一致。

模式2记录区先写local selection，再直接复用B9C0/B9E0与音效46。链越界只在B9C0原始`node->next`读取前typed-stop，保留local selection。模式3按22像素行扫描物品31..33的非零存在性并换算可用索引；完整域均无命中时在下一次原始越界查询点typed-stop，已完成查询保留。仅退出位在命中模式3行域时先退出，不提前访问C090。

公共fallback直接复用C090查询记录15。可用时，模式2按四段严格Y域依次分派445E90、445C90、446260、446090；模式15在特殊控制数大于8且X域命中时使用第二套四段Y域，否则按25像素行更新/提交二级列表。每个控制callback后只在LST明确位置重读Y；最后退出位调用446FE0。

未关闭的437300、446FE0及40DC50查询保留在可变input/state typed边界。C3C0、C090、B9C0、B9E0以及现已关闭的445C90/445E90/446090/446260/446420/446700均已从opaque边界回收为direct typed调用；方向、模式后退和七处提交helper停止均立即传播，通用方向、hover和commit端口已删除。

UT覆盖模式500委派、17/18归一、10/11/5矩形提交、overlay后四owner重读、31特例、普通选择、hover双公式、记录变化与链停止、模式3存在性扫描与完整域停止、模式3行内退出、两套四控制Y重读、模式15行更新/提交、C090越界及不可用后退出。独立ASan通过。

workpack双生成稳定为`125/227`，SHA256均为`0808be422269788e33bac26c514ad271596061bee74f30e389c1a6c16dd732b5`；下一单元`0x00445C90`。Linux完整门结果见最终验证；按阶段门禁不运行Windows BUILD。
