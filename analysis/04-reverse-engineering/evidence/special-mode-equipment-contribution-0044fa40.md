# 汇总指定记录在玩家链与固定装备槽中的贡献 `0x0044FA40`

状态：`platform_adapted`

## 1. LST锁

权威范围为`swd3.exe.lst`的`0x0044FA40..0x0044FAED`，106行，无外部FUNCTION CHUNK。caller为`0x0044E330`和`0x0044EA60`；唯一callee是角色存在查询`0x0040DC50`。

先遍历玩家记录链。记录ID `+4`零扩展后与完整u32目标比较；匹配时把`+8`数量A和`+0A`数量B分别按i16符号扩展，以32位回绕累计。

若打包模式低两位任一置位，到此直接返回，不查询角色或固定槽。低两位均为0时，依次查询角色ID `0x1E..0x21`。每个存在角色严格扫描其16个固定槽；每槽先读记录指针，再读ID。匹配时只把`+8`数量A按i16加入，不读取数量B。四名角色均执行存在查询，只有存在者读取16槽。

## 2. typed-stop

- 玩家链在重复节点读取点停止，保留此前累计。
- 固定槽span过短时只在原槽指针读取点停止。
- 固定槽为null时只在原记录ID读取点停止。
- 两类固定槽停止均保留此前玩家链贡献、存在查询和已完成槽贡献。

## 3. 验证

UT覆盖玩家匹配项两值与两名存在角色固定槽的正负贡献合计6；低位模式跳过全部角色查询；玩家自环；短槽表；null槽。并验证角色查询顺序、玩家/固定槽访问数和停止前缀。

workpack双生成稳定为`200/227`，SHA256为`d8587869e866e10ab7fecf6f54c20416de8e6c6c6219aa4e6e4ae54c7b4de619`。`0x0044DBC0`继续等待callee闭环；下一依赖叶子为`0x0044F800`。
