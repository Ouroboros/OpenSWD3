# 模式1备用输入快照分派 `0x0044F920`

状态：`platform_adapted`

## 1. LST锁

权威范围为`swd3.exe.lst`的`0x0044F920..0x0044FA3B`，174行，无外部FUNCTION CHUNK。唯一caller为`0x0044EA60`。函数自身不实现业务动作，严格按优先级tail-dispatch到已关闭E9D0、DF00、DFF0、E0C0、E1B0、E260、E330和E4A0 typed入口。

## 2. 优先级

顺序固定：

1. 两组退出。
2. 单步前进。
3. 单步后退。
4. 分页前进。
5. 分页后退。
6. 左向减少。
7. 右向增加。
8. 两组确认。

第一个命中立即返回，不检查后续输入。

退出要求active和phase都精确等于1。确认要求active非0且phase精确等于1。

## 3. 方向重复门

四个移动/分页方向使用相同规则：active非0，且phase精确为1，或phase低位为0且按i32大于7。奇数长按阶段即使大于7也不触发；符号位为1的值按i32不大于7。

E260减少和E330增加保留原始陈旧AL行为：

- 当前按钮phase仍用于精确1和signed大于7比较。
- 偶奇门不读取当前phase低位，而读取此前无条件载入的“分页后退phase”低位。
- 因此分页后退phase为奇数时会抑制当前phase=10的减少/增加；其为偶数时同一输入会触发。

必须由单次可变输入snapshot表达，不能改成每按钮独立规范化重复规则。

## 4. typed分派与验证

每条命中路径直接调用已关闭typed helper并传递MAPS、工作区、玩家链、固定槽、掩码、容量、样本owner、运行资源和属性基线；callee停止映射为`callee_stopped`，保留其已提交前缀。

UT覆盖退出压过前进；phase10触发DF00；phase9跳过后继续到DFF0；分页后退phase9抑制E260后落到确认；phase8允许E260；相同陈旧低位允许E330。所有断言均验证实际被调callee的状态变化，而非只检查枚举。

workpack双生成稳定为`212/227`，SHA256为`30a071d40939c431bbfedf6130a0b2fbd540ce8278c719f42bf826c9fd5a6bc9`。下一单元为回收主输入分派`0x0044DBC0`。
