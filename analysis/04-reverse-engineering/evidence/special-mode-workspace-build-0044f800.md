# 筛选来源链并按解析键建立特殊模式工作区 `0x0044F800`

状态：`platform_adapted`

## 1. LST锁

权威范围为`swd3.exe.lst`的`0x0044F800..0x0044F8DC`，126行，无外部FUNCTION CHUNK。caller为`0x0044E4A0`；唯一callee `0x0044FF90`已关闭并直接复用typed helper。

输入是来源链前驱哨兵，不是head值。入口先清工作区head和可见数。随后只处理哨兵next：

1. 每条来源记录无条件清raw `+6`临时word。
2. 仅当打包模式低两位精确等于1，raw `+0x52`权重为0或raw `+0x2C` bit6置位的记录留在来源链；其他低两位值不启用该过滤。
3. 接受记录从来源链摘下，按已关闭FF90解析raw `+0x2C`得到u32键。
4. 工作区使用清零块内`+0x2C`作为首个前驱键0，寻找`previous_key < source_key <= current_key`位置；找不到则尾插。
5. 相等键的后来记录插在既有相等键之前。

结果工作区按解析键升序；来源哨兵链只保留模式1跳过记录的原顺序。可见数始终为0，本函数不重新统计。

## 2. typed-stop

- 来源链在即将重复读取节点时停止，保留此前清word、跳过或摘取插入前缀。
- 工作区搜索在即将重复读取节点时停止，当前来源记录尚未摘链，已完成的工作区保持。

## 3. 验证

UT覆盖键3/1/2重排为1/2/3并摘空来源链；同键后来者前插；模式1保留权重0和bit6记录但仍清临时word；模式2不启用过滤；模式1自环保留首次跳过前缀。

workpack双生成稳定为`201/227`，SHA256为`961d721dba66b2234658638cfbdf02e3887a663629f606881d77a2813232feca`。`0x0044DBC0`继续等待callee闭环；下一依赖单元为`0x0044FAF0`。
