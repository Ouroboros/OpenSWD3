# OpenSWD3 工作约束

本文件只记录长期有效的仓库规则。

阶段进度、当前断点和临时结论必须写入当前执行 PLAN、模块工作包、证据文档或 inventory，
不得塞进本文件。

## 1. 沟通与任务边界

- 默认使用中文沟通。除非用户明确要求其他语言，文档、提交信息、TG 汇报和解释均使用
  中文。
- 只处理用户当前明确要求的目标。用户只提问时只回答，不修改文件、不运行无关命令。
- 请求范围不明确时先问清楚，不猜测、不自行扩大范围。
- 用户要求暂停、`STOP` 或等待时，立即停止所有后续操作。不得趁暂停继续分析、构建、
  提交或推送。只有用户明确要求继续后才恢复。
- 需要用户执行原版、VM 或其他人工操作时，先把工具、路径和操作步骤准备完整，然后停下
  等待结果。等待期间不转去做其他工作。
- 不用“明白”“有道理”代替实际行动或证据。回答先给结论，句子短，分段清楚。
- 不迎合、不夸赞、不猜测用户意图。只报告仓库事实、命令结果、证据和明确推论。
- Context 被压缩后，从仓库和当前断点继续。不要复述已经汇报过的进度，不要向用户重复
  “Context 被压缩”“接着做”等无信息内容。
- 回答中的本地文件使用仓库相对路径。不要向用户输出 Windows 绝对路径或 `/mnt/...`
  绝对路径，除非用户明确要求完整命令。
- 不使用 Markdown pipe table。必须表达表格时，使用代码块中的对齐纯文本表格。

## 2. 开始工作前的读取顺序

修改任何文件前必须读取：

1. 本文件。
2. `~/.codex/memories/writing_style.md`。
3. `~/.codex/memories/collaboration_preferences.md`。
4. 用户明确指定的当前执行 PLAN。
5. 当前模块工作包、相关 inventory 和证据。
6. 将要修改的源码、测试或文档当前内容。

不得从记忆直接修改文件。

仓库同时可能存在标准 PLAN 与 `-pi` PLAN：

- 标准执行对为 `goal/execution-plan.md` 与
  `goal/story-vm-closure-plan.md`。
- Pi 执行对为 `goal/execution-plan-pi.md` 与
  `goal/story-vm-closure-plan-pi.md`。
- 以用户或当前 Goal 明确指定的 PLAN 为准。不得根据修改时间猜测，不得把两套 PLAN 的
  进度、断点或协调规则混在一起。
- 启用 Pi PLAN 时，严格遵守其主 Agent、fresh context、单写者、独立审查和验收规则。
  未明确启用时，不自行切换到 Pi 流程或主动扩展为多 Agent 工作。
- `README.md`、HANDOFF、聊天摘要和旧研究文档都不是进度真值，不能覆盖当前 PLAN 与
  inventory。

## 3. 唯一逆向真值

- `swd3.exe_export_for_ai/swd3.exe.lst` 中的机器码字节和指令是唯一行为真值。
- IDA 反编译 C 伪码只用于辅助阅读和导航。与 LST 冲突时无条件以 LST 为准。
- ASM 不包含 LST 缺失的行为信息，不是函数范围判定的前置输入。
- IDA/FLIRT 可能把游戏自有函数误识别为 CRT 或库函数。任何库归属都必须由实际指令、
  调用者和数据访问验证，不能相信名称。
- 已有完整 IDA export 时，优先使用 LST、调用目录、交叉引用和现有 evidence。不要习惯性
  使用 `objdump` 重做已有信息；只有导出缺失、字节冲突或必须独立验证 PE 事实时才使用。
- 函数名、伪码变量名、字符串解释、旧文档叙述、现有 C++ 和测试通过都不能单独作为实现
  依据。

## 4. 还原目标与允许的适配

- 初步还原追求 bug-for-bug 的 1:1 可观察行为兼容。
- 不修复游戏逻辑 BUG，不整理看似不合理的原始顺序，不现代化整数溢出、随机调用、等待、
  帧内时序、像素结果或异常分支。
- 只有阻断启动或现代系统兼容的问题，才允许在平台边界做最小、隔离、可验证的适配。
- 平台适配必须记录原行为、现代失败原因、最小差异和验证证据，不能悄悄改进业务逻辑。
- 全局变量不能只按地址机械搬运。必须恢复可观察字段、位宽、布局、生命周期、所有权和
  跨模块借用关系；只有 ABI、文件格式或可观察行为要求时才强制保持物理布局。
- 不因当前资产未命中某分支就删除它、伪造成功或标记不可达。不可达必须有汇编与调用域
  证据。
- 总体架构和模块分工明确后，立即按模块边逆向、边实现、边写 UT、边对照汇编。不得重新
  开启无限总体调研，也不得先研究完全部业务再实现。

## 5. 汇编到实现的固定闭环

每个函数、handler 或紧耦合行为单元必须执行完整闭环：

```text
锁定 LST 物理范围、ABI、调用者、全局状态和全部出口
→ 不看现有 C++，独立记录基本块、分支、读写和调用顺序
→ 从汇编独立推导正常、边界、错误、哨兵、截断、符号扩展和回绕测试
→ 再审计现有 C++ 和测试
→ 最小实现或修正
→ LST 到 C++、C++ 到 LST 双向逐基本块追溯
→ 发现差异时同步修正实现、测试、规格、证据和 inventory
→ 从函数入口重新核对全部出口
→ 真实资产、固定状态和可用的原版差分验证
→ 最后一轮完整正反向追溯不再产生差异或未决项
```

- 核对次数不设上限，以结论收敛为停止条件。额外核对一次、固定轮数或 UT 通过都不能
  证明完成。
- 条件方向、数据位宽、符号/零扩展、位运算、整数回绕、重复调用顺序、状态副作用和所有
  返回路径必须逐项覆盖。
- 测试向量必须来源于汇编，不能拿当前实现的假设反向构造。
- `assembly_exact` 只能在零未解释差异、独立分支测试通过并完成双向收敛后使用。
- UT、真实资产和原版动态差分是不同证据等级，不能互相冒充。
- 每关闭一个单元，同时更新实现、测试、evidence、相关 TSV/inventory 和当前 PLAN 状态。

## 6. 固定技术决定

- 语言为 C++20。
- 构建使用 CMake 命令行。默认 Ninja Multi-Config，不依赖 Visual Studio IDE。
- 编译器允许 MSVC 或 LLVM Clang，工程不得硬编码为只能使用某一个编译器。
- SDL3 是窗口、输入、呈现和平台能力边界。业务核心不直接依赖 DirectDraw、DirectInput
  或其他旧 DirectX API。
- 游戏内分辨率固定为 `640×480`。宿主窗口可缩放，内容保持等比，窗口普通尺寸与最大化
  状态由 TOML 保存和恢复。
- OpenSWD3 配置使用 TOML，不使用 INI。
- 游戏数据目录支持命令行和 TOML；测试不得要求 EXE 必须位于原游戏目录。
- 剧情和原始数据仍按字节与字节偏移解释。脚本编码由 TOML 选择：`big5` 对应 CP950，
  `gbk` 对应 CP936，默认 `big5`，不自动猜测。
- 解码后的公共文本内核使用 `char16_t`/UTF-16，不使用平台宽度不同的 `wchar_t`。
- 原版字体基线是正确繁体环境下的经典 `細明體`。12/16/20 三字号的 legacy glyph atlas
  是确定性兼容资产；不要用宿主实时字体结果替换 1:1 基线。
- 音视频最终通过项目自有 `libffmpeg` 动态库提供。主程序只依赖 OpenSWD3 的媒体 ABI，
  不把 FFmpeg API 散布到业务代码。
- 日志是调试基础设施。每条日志至少包含毫秒时间、级别、`file:line` 和消息，并保持
  落盘、刷新和失败回退能力。日志不能改变原逻辑时序与返回合同。

## 7. 命令、平台与构建

- 搜索、读取、分析、编辑、Git 和普通开发默认使用 Bash。
- 搜索文件和文本优先使用 `rg --files` 与 `rg`，不要用 Windows `findstr` 代替。
- 只有 Bash 无法完成的 Windows 专属操作才使用 Windows CMD，例如执行 `build.bat`、
  启动 Windows EXE 或验证 Windows 窗口行为。
- 不要把所有命令强行改成 Windows CMD，也不要因为一次需要 CMD 就在后续文件操作中继续
  使用 CMD。
- `build.bat` 必须保持 CRLF，且不得加入 `pause`。
- Linux 使用 `build.sh`；Linux 与 macOS 可复用相同的 POSIX 构建思路，但没有实际 macOS
  验证时不能声称已经支持或通过。
- Linux与Windows统一使用Ninja Multi-Config。Linux首次构建或generator不匹配时configure；有效Ninja
  cache存在时直接build，由Ninja在CMake输入变化时自动重配，不得恢复每次无条件configure；主动更换
  compiler、preset关键变量或脚本传入的CMake选项时，设置`OPENSWD3_RECONFIGURE=1`强制重配。
- `build.sh` 与 `build.bat` 的编译和 CTest 默认都使用当前在线 CPU 数量的并发 job。用户未明确要求时，
  不得设置 `OPENSWD3_BUILD_JOBS` / `OPENSWD3_TEST_JOBS`，不得自行降为 `-j2` 或其他并发数。不得移除
  同测试二进制调用锁或真实资产全局锁来换取虚假提速。
- Linux core/app完整门禁只能分别通过`./build.sh core`和`./build.sh app`执行；不得手写展开脚本内部的
  CMake build和CTest命令。
- 修改后运行仓库现有的定向测试和适用的 Linux/Windows 完整门禁。命令没有实际执行时，
  不得声称已构建、已测试或已通过。
- 构建成功不等于运行验证成功，测试枚举数量也不等于测试实际通过。

### 7.1 受管进程安全

- 任何可能阻塞、等待输入、无限循环或完成时间未知、无上界的命令、脚本、构建、测试、
  server、watcher、tail、REPL 或其他进程，都不得在前台启动。
- 这类非交互进程只能通过受管后台进程设施启动；使用 `process` 管理非交互进程，只有确实
  需要输入、批准或现场监督的交互式命令才使用 `interactive_shell`。
- 不得使用前台 Bash、`ctx_execute`、subagent host command 或同类执行路径启动可能阻塞的
  工作，也不得用 `&`、`nohup`、`setsid`、`disown` 或 shell job control 模拟受管后台执行。
- 按用户明确要求，不得给受管命令、构建、测试、脚本或Git命令增加`timeout`包装，也不得在工具调用中
  另设执行超时；只配置完成或失败通知，并通过受管设施的停止能力处理异常。启动后不得`sleep`、轮询
  或占住当前回合；依赖受管通知返回结果。
- 前台执行仅限确定有界、非交互、单次完成的命令，不得额外包`timeout`。
- Git操作不受必须使用受管后台设施的限制，可以在前台执行；按用户要求同样不得增加`timeout`包装。
- 如果受管执行设施不可用，或无法确定进程是否有界，必须 fail closed：不得启动进程，
  应停止并向用户确认如何继续。

## 8. EXE 与原版动态验证

- 未经用户明确许可，不启动任何原版或 OpenSWD3 EXE。构建和 CTest 不包含启动授权。
- 不因需要观察行为就自行启动原版。先判断静态汇编、UT 和资产验证是否足够。
- 确实需要原版动态值时，准备 Frida spawn 一键工具，再由用户执行。
- VM 目标环境默认没有 Python、Frida 或开发工具。交付必须是可复制到独立子目录的一键
  便携目录，不能把大量散文件直接丢进游戏根目录。
- 当前统一工具以 `analysis/tools/swd3-oracle/README.md` 为准。历史 attach 产物只作为
  已归档证据；后续工具默认使用 spawn，除非用户明确要求改回 attach。
- 工具、目标 EXE、输出目录和操作步骤必须写清楚。用户返回输出后再继续，不得边等用户
  边推进其他实现。
- 不纠结 `swd3.exe`、`swd3_nodvd.exe` 等补丁名差异，除非当前证据或用户明确要求区分。

## 9. 文件、资产与生成物

- 所有新增源码、文档、测试、工具和整理后的证据统一放在 OpenSWD3 仓库内分类保存。
- 逆向证据放 `analysis/04-reverse-engineering/`，工具放 `analysis/tools/`，执行 PLAN 放
  `goal/`，构建和 VM 临时输出放 `build/`。
- 原始游戏数据、EXE、用户 VM 输出和构建产物不得误提交。
- 用户返回的原始 capture 先放 `build/vm/`。只有最小、来源明确、可复核的证据才整理进
  `analysis/04-reverse-engineering/artifacts/`。
- 图片、位图、字体和其他资产必须有来源记录。不得生成或引入来源不明的替代素材。
- `__pycache__/` 必须全局忽略且不得被 Git 跟踪。生成 Python 工具时也要保持这一点。
- 不维护、不生成、不讨论旧 HTML/INDEX，除非用户明确要求。当前执行状态以 Markdown
  PLAN、模块文档和 inventory 为准。
- 不删除、覆盖或批量改写用户已有文件。需要清理物料时先精确列出目标和可恢复性。

## 10. 代码与文档风格

- 项目自有 C/C++ 源码、头文件和测试统一四空格缩进，不使用 Tab。
- 以根目录 `.clang-format` 和 `.editorconfig` 为准；C/C++ 使用 UTF-8、LF、80 列目标。
- `{}` 代码块结束后留一个空行，除非该 `}` 紧接另一个 `}`。
- `switch` 的不同 `case` 之间留空行。
- 按职责和抽象层级组织代码。通用入口、共享 helper、业务实现和专用转换不要无序交叉。
- 修改必须最小且聚焦。不要顺手重构、重命名、重新设计接口或增加无关测试。
- 不添加 `any`、`ts-ignore`、`eslint-disable`、占位 TODO、dummy 实现或为了过测试的伪造
  逻辑。
- 不添加没有明确合法来源和业务语义的“安全”空值检查，也不能用默认零值隐藏调用链错误。
- 文档写事实、证据地址、验证等级和未决项。不要把推测写成完成结论。

## 11. Git、阶段提交与工作区保护

- 工作区可能包含用户改动。编辑前读取当前文件和 diff，只处理本阶段相关内容，不覆盖或
  混入无关变化。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理工作区。
- 不使用 `git -C`。Git 命令通过工具的仓库 `workdir` 执行。
- 每到一个已经验证、可独立回退的阶段边界，必须自动完成：

  ```text
  精确暂存本阶段文件
  → 使用 $commit Skill 检查并提交已暂存内容
  → 确认暂存区为空
  → 立即 git push
  → 继续下一阶段
  ```

- `$commit` Skill 本身不负责暂存或推送。主 Agent 在调用 Skill 前只暂存本阶段文件，
  Skill 完成后再推送。
- 不绕过 `$commit` Skill 直接执行 `git commit`。
- 不等待用户提醒提交或 push。只有用户明确要求“下次提交后暂停”时，完成该次提交和 push
  后暂停；用户直接要求暂停时立即停止，不补做提交。
- 测试失败、证据未收敛或改动跨越未完成边界时不得抢先提交。
- 提交前检查 staged diff 和近期历史，提交后确认实际 commit 与远端结果。不得声称提交或
  push 成功，除非命令确实成功。

### 11.1 提交标题必须让玩家和普通开发者直接看懂

- 提交标题必须根据本次实际代码、测试、原版文字和可观察结果逐条编写，禁止批量替换词语、
  套模板或只改动动词。
- 标题必须回答以下三个问题；缺少任何一项时不得提交：
  1. 改的是哪个真实游戏界面、游戏功能或明确的数据处理职责；
  2. 玩家做了哪个具体操作，或程序在什么明确时机执行；
  3. 画面、数据或运行结果具体发生了什么变化。
- 已确认的游戏名称必须直接使用，例如：炼妖、东西祭坛、护驾、装备、物品栏、炼妖壶、
  标题画面、读取进度、游戏设置、角色属性、增加道具。不得用“特殊功能”“某界面”之类
  自造的笼统名称替代。
- 共用内部函数确实不属于单一界面时，标题必须写清实际数据操作，例如“统计菜单列表中的
  项目数量”或“从 MAPS.DAT 读取菜单项目说明文字”，不得虚构一个业务页面。
- 禁止在标题中使用不能独立说明结果的内部黑话，包括但不限于：`闭环`、`特殊模式`、
  `标准模式`、`分派`、`编排`、`前进`、`后退`、`当前选择`、`配对`、`transition`、
  `runtime`、`owner`、`helper`、`entry`、`page`、`mode`。确需描述底层实现时放入提交正文或
  证据文档，不得用它们充当标题的业务内容。
- “恢复”“完成”“处理”“准备”“更新”“支持”等宽泛动词不能单独承担标题含义。必须继续
  写明具体操作和结果，例如“点击装备界面下一页后显示后24件物品”。
- 不得根据草拟的提交标题反过来修改代码行为。代码和权威LST决定标题；标题与代码不一致时，
  应修正标题。只有代码中的接口或变量名称本身错误、容易误导时，才另开独立重命名改动，并
  保证不改变行为。
- 如果真实界面、按钮或结果尚未查清，必须继续检查原版字符串、调用者、测试和证据；不得先
  发明名称并提交。
- 提交前必须人工阅读完整 staged diff，并用一句普通中文复述“玩家或程序能观察到什么变化”。
  复述不清楚，或仍需要解释标题中的名词时，标题不合格。
- 合格示例：
  - `feat: 点击装备界面下一页后显示后24件物品`
  - `feat: 点击炼妖确认后比较两座祭坛并生成结果`
  - `fix: 修正高刷新率下人物移动插值的时间基准`
  - `docs: 记录 Map_Eu08.mp3 在 Windows 上连续循环两轮的复测结果`
- 禁止示例：
  - `feat: 闭环标准特殊模式总入口`
  - `feat: 后退特殊模式转场配对模式`
  - `feat: 确认转场界面的当前选择`
  - `docs: 关闭 FFmpeg 媒体运行时复测`

## 12. 进度与 TG 汇报

- 正常开发只汇报新进展、实际结果、阻塞和下一步。不要反复输出完整整体进度，除非用户
  明确询问。
- 每达到一个已经验证、可独立回退的阶段性边界，并由主 Agent 检查后，必须发送 TG：

  ```bash
  python3 /mnt/d/Dev/Source/Project/stockkit/scripts/tg_notify.py "CONTENT"
  ```

- `CONTENT` 必须是高层、简短的中文阶段摘要，只包含阶段/工作包名称、完成内容概述、整体
  进度、验证结果和下一步；只有确有阻塞时才增加剩余风险。完成内容只概括模块或业务结果，
  不展开代码实现细节，不罗列 helper、字段、flag、地址分支或测试边界。汇报必须基于当前
  仓库实物和验证证据。
- TG 消息固定为五段。必须逐字使用下面的段首和顺序，不得改标签、重排、增减段落、合并
  段落或临时改写成另一种汇报风格。每段之间恰好保留一个真实空行：

  ```text
  OpenSWD3 <模块或阶段>：<功能或工作包>已完成。

  实现：<高层业务结果>。

  兼容：<关键兼容行为与调用方回收；没有新增兼容事项时写“保持既有兼容边界。”>。

  验证：<实际执行的验证门>全部通过。

  进度：<整体进度>；下一项<下一工作包或明确动作>。
  ```

- 当前模块10战斗工作包的验证段固定写成：`验证：定向测试、AddressSanitizer、Linux core
  188/188、Linux app 194/194 全部通过。`只有验证集合或测试总数发生真实变化时才允许修改。
- 五段模板之外不得增加标题、问候、总结、风险或备注。确有阻塞时，把阻塞事实压缩写入“兼容”
  段；不得临时增加第六段。
- TG 使用普通双引号，不使用 `$"..."`，不放奇怪控制字符。
- TG 命令失败时记录错误并明确告知用户，不得声称已经发送或送达。

## 13. HANDOFF 规则

- 只有用户明确要求时才生成 HANDOFF，统一放在 `goal/`。
- HANDOFF 必须从当前仓库实物重新核对，不能从聊天记忆或旧摘要直接生成。
- 必须明确当前启用的是标准 PLAN 还是 `-pi` PLAN，不能混用两套断点。
- 至少记录：当前分支与 commit、远端同步情况、未暂存/已暂存/未跟踪文件、当前阶段和
  精确函数/handler 断点、最后实际验证、未完成项、下一条可执行动作和禁止事项。
- 对无法证明的进程状态、测试结果、用户环境或动态行为明确写“未验证”，不得推断。
- HANDOFF 中的路径使用仓库相对路径；写完后机械检查链接、清单数量和 Git 事实。
- HANDOFF 只是交接材料，不得取代 PLAN、inventory、evidence 或 LST。
